package com.kafka.stream;

public class MainTest {
	/*
	 * @Test public void testAppHasAGreeting() { Main classUnderTest = new Main();
	 * assertNotNull("app should have a greeting", classUnderTest.getGreeting()); }
	 */
}
